import { Container } from "react-bootstrap"
import { NavBar } from "../NavBar"
import { Footer } from "../Footer"

export const FinalProjet = () => {
    return (
        <section className="project-page" id="final-project">
            <NavBar fullDisplay={false} />
            <Container>

            </Container>
            <Footer />
        </section>
    )
}